import 'package:flutter/material.dart';

/// The dark navy used for the app icon's pin shape, applied to the app's
/// primary call-to-action buttons for brand consistency.
const brandNavy = Color(0xFF2C398F);
